
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // ACESSO TOTAL LIBERADO - CORREÇÃO CRÍTICA DE PERMISSÃO
    // Esta regra ignora qualquer verificação e permite tudo para todos.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
