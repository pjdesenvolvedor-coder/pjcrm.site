rules_version = '2';

// ATTENTION: Rules are wide open for debugging.
// This allows all reads and writes to all collections.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
