
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Acesso total liberado para evitar erros de permissão em desenvolvimento.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
