'use client';

/**
 * Upsell2MessageHandler
 * 
 * DISPATCH ENGINE IS 100% SERVER-SIDE ONLY (/api/cron/run).
 * Client-side browser tab dispatches are intentionally disabled to eliminate race conditions
 * and duplicate sends when multiple attendants or browser tabs are open simultaneously.
 */
export function Upsell2MessageHandler() {
    return null;
}
